"""Prediction-side metrics and resampling utilities.

Provides the scalar criteria the mode-resolved spectrum is compared against
(:mod:`calibinfo.metrics.spectral_criteria`) and the object-cluster bootstrap
used for every reported interval (:mod:`calibinfo.metrics.cluster_bootstrap`).
"""
