# Claims & evidence registry

Every quantitative claim in this repository's documentation is bound to a
committed evidence file under `results/`. This table is the single index of
those bindings: a reader (or reviewer, or downstream user) can go from any
number in the docs to the file and field that produces it, and re-derive it
with the listed command.

The enforcement is mechanical: `tests/test_claims_gate.py` blocks
overclaim phrasings, and its `test_readme_numeric_claims_traceable`
requires every number printed
in the README to appear verbatim under `results/**`.

## Library / methods claims

| # | Claim | Value / bound | Evidence | Re-derive |
|---|---|---|---|---|
| M1 | Dual-route equivalence (Schur profiling vs Gaussian marginalization) | elementwise rel < 1e-10 | unit assertion | `pytest tests/test_information_modules.py` |
| M2 | Operator concavity of t ↦ ΔF(t) | midpoint residual ~1e-15 | `results/`-independent | `pytest tests/test_math_foundations.py` |
| M3 | Convexity of J_A, J_E, J_D on the PD feasible region | 0/400 midpoint violations each | unit assertion | `pytest tests/test_math_foundations.py` |
| M4 | Exact low-rank retention identity R = I − VVᵀ | spectral deviation ≤ 1e-10; #ρ≡1 = P − rank(V), at least max(0,P − 3L); equality P − 3L requires full column rank 3L | unit assertion | `pytest tests/test_lowrank_identity.py` |
| M5 | J_A gradient exactness | FD agreement ≤ 1e-6 | unit assertion | `pytest tests/test_math_foundations.py` |
| M6 | Convex-program certificates are global | FW gap ≥ 0; no feasible point beats the bound | `results/certification/certified_gaps.json` | `pytest tests/test_convex_certificates.py` |
| M7 | Singular-covariance semantics | factor/marginal = 0.5, pseudoinverse-precision shortcut = 0 (never equivalent) | unit assertion | `pytest tests/test_math_gates.py` |
| M8 | Retention-covariance theorem | F∞^{1/2} Cov F∞^{1/2}/σ² = R⁻¹ (matched GLS) | unit assertion | `pytest tests/test_math_gates.py` |
| M9 | **[refuted] Historical alpha-only candidate**, not a CR17 theorem | The candidate γ ≥ 1/(1+α) is false: α=1 and exact γ=14/109 < 1/2. At each fixed positive α, a two-dimensional two-update family has inf γ=0. The 162,000 searched triples (including 12,493 inverse-square reversals) remain historical finite-sample records, not a general guarantee. The historical API retains its numeric behavior; the valid full-trace replacement is M12. | `docs/theory/source_20260917/section_gamma.tex`; `results/theory_extension_20260918/imported_20260917/gamma_exact_verification.json` | `pytest tests/test_gamma_general_bound.py` |
| M10 | Universal ceiling on the calibration-budget value | D = 1 − J_A(κ·1)/J_A(1) ≤ 1 − 1/κ; met to ≤ 1e-4 at level 64. **Structural assumptions** (methods.md §7): u_k = A^T B_k, M0_k = B_k^T B_k, F∞ = A^T A from one shared (A,B); a decoupled (u, M0) triple violates the bound (non-legitimate counterexample, D ≈ 0.98 > 0.5 @ κ=2) | `results/magnitude/calibration_value_ceiling.json` | `pytest tests/test_math_foundations.py::test_calibration_value_ceiling` + `::test_structural_link_required_for_ceiling` |
| M10' | Universal ceiling for ANY quadratic functional | tr(H ΔF(t)^{-1} H^T) ≥ tr(H ΔF(1)^{-1} H^T)/t ⇒ V_H ≤ 1 − 1/κ for every H; proof via Loewner monotonicity of X ↦ X^{-1} (unconditional, no extra hypotheses beyond the M10 structural model) | `results/goal_oriented/goal_orientation.json` | `pytest tests/test_goal_oriented.py` |
| M11 | Goal-oriented calibration value is task-dependent | J_H = tr(H ΔF⁻¹Hᵀ); same uniform refinement at level 0.5 buys median 62.87% (all-parameter A-opt, = the P-CERT headline, route cross-check) vs 89.76% (mean-albedo task); at level 0.1: 7.82% vs 87.93%; per-light gain rankings disagree (median Spearman 0.936, min 0.586, top-3 disjoint 11/44 cells). H acts on the per-pixel additive-albedo parameterization used by A_k=diag(sqrt(w_k) s_k). A log-albedo change requires congruence by diag(rho) and a corresponding task transform; the joint unit-normal model has intrinsic dimension 3P, not an unconstrained 4P space (M17). Mechanism: the albedo-weighted mean functional is exactly gauge-aligned with the light-intensity nuisance (c_k = e_1/||rho|| per light), and the two-term law V = (1-1/kappa)/(1+qr) predicts its value curve (median |dV| 0.0008, max 0.039 @ level 0.1; V_rho_mean 0.878 -> 0.9000 along the level grid) | `results/goal_oriented/goal_orientation.json` (+ `gauge_mechanism_rho_mean`) | `pytest tests/test_goal_oriented.py` |

## Continuation claims and their scope

These entries identify analytic derivations separately from finite numerical
checks. New research code is isolated in `experiments/theory_*.py`; its output
is not silently merged with the historical benchmark.

| # | Claim | Status and precise scope | Evidence / proof | Re-derive |
|---|---|---|---|---|
| M12 | Valid full-trace leave-one-out spectral gamma bound | **Proved in the supplied prior derivation; imported and regression-tested.** Fixed SPD space, PSD updates, full trace; zero updates are excluded and the all-zero case has gamma=1. Not an arbitrary rank-deficient-task guarantee. | `docs/theory/source_20260917/section_gamma.tex`; `results/theory_extension_20260918/imported_20260917/gamma_exact_verification.json` | `pytest tests/test_gamma_general_bound.py` |
| M13 | Physical Lambertian failure of the alpha-only candidate | Explicit physical construction and fixed-alpha family; independent proof review and result binding recorded in the continuation index. The class includes per-light SPD coupled priors; narrower prior families are not automatically covered. | `docs/theory/physical_gamma_and_tightness.md`; `results/theory_extension_20260918/gamma_extensions.json` | `pytest tests/test_gamma_extensions.py` |
| M14 | Exact nonnegative remainder and computable enclosure for the two-term law | Unit exact gauge direction, shared Gram model, proper positive-definite prior; fixed dyadic spectral bins bound the remainder without an observed error input. Finite positive direction-only controls and the singular covariance limit are distinguished. | `docs/theory/two_term_remainder.md`; `results/theory_extension_20260918/two_term_remainder.json` | `pytest tests/test_two_term_remainder.py` |
| M15 | Stronger full-trace spectral guarantee and a constrained tight family | Leave-two-out and inverse-order conditioning improvements; exact worst-case statement is restricted to two dimensions, two candidates and fixed baseline condition number, with unbounded update sizes. Simultaneously fixing update scale and kappa is not solved. | `docs/theory/physical_gamma_and_tightness.md`; `results/theory_extension_20260918/gamma_extensions.json` | `pytest tests/test_gamma_extensions.py` |
| M16 | Information-risk ordering with a measurable pairwise margin | Matched quadratic endpoint plus bounded covariance/bias/nonlinear and sampling errors; only separated pairs receive an ordering guarantee. No unconditional Spearman threshold and no automatic angular-error or dataset transfer claim. | `docs/theory/matched_ensemble_and_bridge.md`; `results/theory_extension_20260918/matched_ensemble_bridge.json` | `pytest tests/test_ensemble_bridge.py` |
| M17 | Joint log-albedo/unit-normal information retains ceiling and convex certificate | Intrinsic scene dimension 3P; fixed identifiable quotient, shared Jacobians and fixed task. Proper priors lift calibration gauges; flat priors require a common quotient and may have the full local GL(3) ambiguity. Matched linear and small-noise nonlinear profile covariance checked independently on a synthetic scene. | `docs/theory/joint_information_extension.md`; `results/theory_extension_20260918/joint_information_extension.json`: `dimension_and_gauge`, `rows`, `ceiling`, `certificate`, `acceptance` | `pytest tests/test_joint_information_extension.py` |
| C13 | Matched-ensemble amplitude closure and staged mismatch attribution | S1 noise, estimator, baseline, projection and coordinates all match; S2 changes only the bootstrap denominator; S3 changes the finite forward map. These controlled experiments do not alone decompose the historical image-side ratio in B2. | `docs/theory/matched_ensemble_and_bridge.md`; `results/theory_extension_20260918/matched_ensemble_bridge.json` | `pytest tests/test_ensemble_bridge.py` |
| C14 | **Nuisance-rank (low-rank) computation route for full-resolution information** | The retention operator's nuisance-rank factorisation (`V = F∞^{-1/2}[u_k K_k^{1/2}]_{k∈active}`, P × 3\|active\|) yields full-resolution risks, gradients and certificates **without dense P×P inversion**; dense and low-rank routes agree to **1.248e-15** relative error in the independent joint-model check (M17); unlocks P ≫ 1200 (P = 54600 needs 22 GB dense vs 455 ms low-rank; transcribed from the module docstring into `results/theory_extension_20260918/lowrank_route_measurement.json`) | `results/certification/lowrank_fullres.json`; `results/theory_extension_20260918/joint_information_extension.json`; `results/theory_extension_20260918/lowrank_route_measurement.json`; `src/calibinfo/information/lowrank.py` | `pytest tests/test_m2_m3_evidence.py` + `pytest tests/test_joint_information_extension.py` |

## Real-data benchmark claims (11 held-out OpenIllumination objects)

| # | Claim | Value | Evidence | Re-derive |
|---|---|---|---|---|
| B1 | **Superseded historical ordering headline; current per-sample projection control** | Historical R_A = 0.90, CI [0.7, 0.95], 65/66 cells, 11/11 objects remain traceable but are superseded. Current D arm: **R_A = 0.55, CI [−0.10, 0.70]**, 43/66 positive cells and 7/11 positive objects; the interval includes zero and does not establish robust positive ordering. `gauge_mode=per_seed`, `prediction_field=pred_deg`: this is the original-predictor ordering control, not a validation of matched prediction. | `results/theory_extension_20260918/imported_20260917/mf0_factorial_summary.json`: `variants.D.{RA,RA_ci95,n_pos_cells,n_pos_objects,n_objects}`, `protocol.n_cells`, `protocol.bootstrap` (object bootstrap seed 20260908) | `pytest tests/test_claims_gate.py` |
| B2 | **Superseded amplitude headline; coordinate-matched, ensemble-unmatched comparison** | Historical original-pipeline median 201.1 and corrected legacy-gauge median 15.98 remain provenance, not current amplitude validation. With per-sample projection, D-arm empirical / same-coordinate prediction has median **53.744**, p5 **0.489**, p95 **820.342**, across 330 condition-mode entries. The denominator remains **residual bootstrap**, not the ideal matched GLS baseline; this ratio alone neither validates nor refutes the theorem. The historical synthetic matched MC 1.0045 is a separate ensemble. | `results/theory_extension_20260918/imported_20260917/amplitude_comparison.json`: `variants.D.new_ratio_matched_prediction.pooled.{median,p5,p95,n}`, `bootstrap` (descriptive amplitude seed 20260917) | `pytest tests/test_claims_gate.py` |
| B3 | Stratified severity branch retired | arm-D stratified −0.495 vs frozen +0.536; flip isolated to the noise-fit correction | `results/openillumination/correctness/mf0_factorial_summary.json` | `pytest tests/test_factorial_evidence.py` |
| B4 | Policy-comparison nulls are structural | certified epsilon vs worst policy deviation, per k (median certified dynamic range 0.6287 vs random-mean-minus-lower-bound): k=5 → 125×, k=10 → 98×, k=14 → 127×, k=28 → 247×; at k=48 the random mean attains the lower bound (gap 0, ratio undefined). A magnitude gap per-k, not a pinned constant; the envelope's absolute scale is set by the Finf heterogeneity tail | `results/certification/certified_gaps.json` (`by_k`) | `pytest tests/test_certified_gaps_evidence.py` |
| B5 | Allocation benefit is an active-set effect **on the E_osb endpoint** | On the model-space weak-mode endpoint (E_osb = the per-level×seed mean of ‖e·V_level‖², the gauge-aligned residual projected onto the predicted weak-mode subspace — the frozen endpoint of the producer): Δ(mode − random_active48) = +0.014/+0.019, CIs span 0, 3/11 improved, i.e. the mode ordering adds no detectable advantage. **Endpoint-scoped, not universal:** on the physical normal-angular-error endpoint the within-active-set advantage is real with CIs excluding 0 (−0.38…−1.40 on the feasible interval) — see B9. The two functionals do not share a ratio (same qualifier as C8) | `results/openillumination/allocation/allocation_random48_summary.json` | `pytest tests/test_benchmark_evidence.py::test_N11_random48_control_ci_spans_zero` |
| B6 | Linearization validity radius (v2: observation-side injection + nominal-geometry estimator) | radius_2x median 1.0 (11/11 crossed, per-object 0.5–1.5); radius_10x median 1.5 (per-object 1.5–2.0); small-level log-log slope +2.02–2.19 (v1: −1.9, withdrawn as estimator-collapse artifact) | `results/magnitude/linearization_radius.json` | `pytest tests/test_pradius_pconc_artifacts.py` |
| B7 | Certificate concentration: two distinct uncertainty quantities | rel_spread_median 1.08% (within-family IQR); clean_vs_mean_ratio median 1.42, min 1.17, max 3.08 (systematic noiseless-vs-noisy calibration-source offset: +16.8% to +208.2%, median +42.2%). Reported separately; rel_spread is not the total bound accuracy | `results/certification/certificate_concentration.json` | `pytest tests/test_pradius_pconc_artifacts.py` |
| B8 | **Historical validity-map description only; not a current projection-pipeline guarantee** | The frozen legacy-gauge map has 66 cells (11 objects x 6 levels), with median Spearman >= 0.8 and Kendall >= 0.7 in its excess-over-first-order-scaling bins; magnitude median-of-medians 7.7 -> 10.4 -> 72.0 -> 106.5, and level 0.2 Kendall 0.6. These historical values are retained for traceability. The per-sample projection revision in B1 does not support carrying the old robust-ordering conclusion forward; the updated matched-ensemble experiment must be assessed separately. | `results/magnitude/validity_map.json` (frozen historical map); current control in B1 | `pytest tests/test_validity_map.py` |
| B9 | Decision quality (v1.1, active-set control in-grid; feasible-budget corrected): the information allocation beats BOTH random baselines on the physical endpoint; on the **feasible budget interval** the within-active-set advantage is ~2.5× smaller than the universe-random advantage, not an order of magnitude | normal angular error, dAUC vs universe-random (full grid): a_opt −3.90 [−6.52, −2.45] @10, −5.03 [−7.92, −3.51] @100; mode_aware −3.68/−4.84; e_opt −3.88/−5.14; d_opt −3.61/−4.75 (all CIs exclude 0). dAUC vs active48-restricted random (the honest test): −0.17 to −0.55 on the FROZEN full budget grid — but k=57/85/114 exceed the active-set size 48, where both sides' prefixes select the same lights and the endpoints coincide bit-exactly (88/88 cells per budget), zeroing 3 of 5 budget intervals: the full-grid values are DILUTED 2.0–2.8×. On the feasible interval (k ≤ 48, fractions [0.1, 0.2]) the honest within-active-set advantage is −0.38 to −1.40 (all CIs exclude 0) and its ratio to the feasible universe-random advantage is 0.27–0.51 — a factor ~2–3.7, NOT an order of magnitude (`decision_quality_feasible.json`, P-DQ-FEASIBLE: non-degeneracy flags + feasible/ full dAUC). The several-degree headline is still mostly the active-set effect (C8); within the active set the advantage is real and ~2.5× smaller than the headline. **Endpoint note (B5):** on the E_osb model-space weak-mode endpoint that within-active-set advantage is NOT detectable (CI spans 0) — the two endpoints disagree, which is why both are reported rather than one being quoted as the general answer. Spearman(predicted J_A, realized) positive in 88/88 cells (median +0.917); informed-only (n=4 family): pooled pairwise sign agreement 678/987 = 0.687 [0.657, 0.716], median Spearman +0.800, min −1.000 (at least one cell fully rank-reversed — the counterpart of the 88/88 headline). Gauge-aligned endpoints still show the redistribution phenomenon (median Spearman −0.575 MSE / −0.525 dual). Validation: the 2200 unchanged-unit rows (8800 values) are BIT-EXACT vs the v1 artifact read from git history (full-magnitude comparison in the reuse-equivalence record) | `results/openillumination/decision_quality.json` + `results/openillumination/decision_quality_feasible.json` | `pytest tests/test_decision_quality.py tests/test_dq_feasible.py` |

| B10 | Absolute DiLiGenT ground-truth accuracy under calibration refinement (Q4): for the stated full-mask estimator, perturbation family and 10-object cohort, more calibration precision moves the endpoint toward its exact-calibration result — a fixed-protocol absolute endpoint check, **not** a universal ordering advantage and not a reconstruction guarantee | Cohort-median full-mask mean angular error (mode_aware) 18.9681° at k=0 → 16.4146° at k=48, against the exact-calibration nominal floor 16.0175°; predesignated bearPNG error-map mean 13.0746° → 11.3461° at k=28 (per-pixel mean over the 10 paired corruption seeds). Object-median mode_aware − active_random differences +0.0203° / −0.0372° / +0.3706° at k=5/14/28 (negative favors mode-aware — sign changes at intermediate budgets); policies coincide at k=0 and k=48 | `results/diligent/diligent_gt_accuracy.json` (`summary.cohort_curve`, `summary.paired_policy_difference`, `rows`); loader pinned by `provenance.loader_sha256` = `src/calibinfo/datasets/diligent.py` | `pytest tests/test_diligent_gt_accuracy.py` |

## Certified-allocation claims

| # | Claim | Value | Evidence | Re-derive |
|---|---|---|---|---|
| C1 | Certified dynamic range, **P=1200 subsample (secondary cross-check; superseded as headline by C3)** | 36–89% per object (median 62.87%) | `results/certification/certified_gaps.json` (`display`) | `pytest tests/test_certified_gaps_evidence.py` |
| C2 | Greedy is certified essentially optimal | 0.011–0.028% above the convex lower bound at every budget | `results/certification/certified_gaps.json` | `pytest tests/test_certified_gaps_evidence.py` |
| C3 | **Full-resolution headline** (all masked pixels P = 3559–10252, all 142 lights): calibration value and allocation certificates | dynamic range **27.328%–89.392%, median 60.056%**; greedy gap median 0.002049–0.004917%, **max 0.020113%**; active-set-random gap median 0.138952–0.212675%, max 1.142090% | `results/certification/lowrank_fullres.json` (`display`, `by_k`) | `pytest tests/test_m2_m3_evidence.py::test_fullres_structure` |
| C4 | **Superseded** — the v1.0 "targeted intervention is significant" claim was a gauge-alignment asymmetry artifact; the corrected three-arm rerun (same file, `aggregated`) finds no mode-specific advantage **on the bottom-5 dual-coordinate energy endpoint** (the shared `arm_energy` pipeline): targeted is never better than random and shows no increment over the scalar arm. Endpoint-scoped, not universal — the same framework's physical-endpoint advantage is real (B9) | `results/mode_tail/allocation_mode_tail.json` (`erratum`, `aggregated`) | `results/mode_tail/allocation_mode_tail.json` | `pytest tests/test_m2_m3_evidence.py::test_alloc2_structure` |
| C5 | Allocation rank invariance | all 594 frozen orderings keep numerical rank 1200 = rank(F∞) at every budget prefix | `results/openillumination/correctness/allocation_rank_check.json` | `pytest tests/test_math_gates.py` |
| C6 | Dynamic range is a curve in `level` (not a single number) | median D: 0.7% @ level 0.025 rising to 89.85% @ level 8.0 (37× span); median 62.87% @ the P-CERT operating point level 0.5; saturates toward the universal ceiling 1−1/κ = 90% at large level | `results/certification/certified_gaps_levels.json` (`by_level`) | `pytest tests/test_certified_gaps_levels.py` |
| C7 | The calibration-budget value is carried by the intensity channel **(caveat: within the joint parameterization's intensity-dominated error ratios — see C10)** | direction channel contributes ≤ 2% at every level/object (max 1.68%, obj_19_cylinder @ level 0.2); intensity-only reproduces the joint dynamic range (median |joint−intensity| < 0.05 pp at level 0.5). **C10 qualification:** this holds wherever the intensity variance dominates the error profile; at the measured operating point of a real sphere calibration (direction variance 10.6× intensity) the direction channel carries a median 36% — the claim is channel-conditional, not universal | `results/openillumination/channel_decomposition.json` (`by_channel`, `display.direction_max_pct`) | `pytest tests/test_channel_decomposition.py` |
| C8 | Active-set ablation: the informed-vs-random advantage IS the active set; the weak-mode endpoint is non-monotone in coverage | On J_A (11 objects x 5 budgets, 55/55 rows): active-set dilution +0.011..+0.298 (median 0.063) while mode-ordering gap (|gap| <= 0.004), continuous-to-rounded loss (<= 0.0005) and the certified gap (<= 0.0008) are orders smaller — on the J_A functional the entire advantage over universe-random is the active-set effect. **Budget-axis caliber (P-ABLATION-FEASIBLE):** the quoted ratio uses the full grid including k=48 = |active|, where the ordering sub-term is definitionally zero (11/11 rows) and therefore dilutes the ratio to ~1364×; excluding k=48 it is ~1001× — both order-of-magnitude, conclusion unchanged (`active_set_ablation_feasible.json`). **Functional qualifier:** on the REALIZED normal-angular-error endpoint the ordering carries 40–45% of the within-active-set gain (feasible-interval dAUC ratio 0.27–0.51; B9) — the two functionals do not share a ratio, and this claim is stated for J_A only. On the realized weak-mode endpoint the E(k) curve is NON-MONOTONE (legacy frame, frozen-anchored bit-exact: obj_10 1.07 -> 1.46 @ k=14 -> 0.19 @ k=48; obj_03 0.59 -> 1.21 -> 0.20): partial refinement removes gauge-parallel error (mae improves, e.g. 0.12 -> 0.06) while redistributing into the aligned weak modes, and the stable 100-seed oracle ranking ANTI-correlates with single-light J_A gains (Spearman -0.36 / -0.82, split-half 0.82/0.88) — J_A gains predict total information, not weak-mode energy (decision-level validity envelope) | `results/openillumination/active_set_ablation.json` + `results/openillumination/active_set_ablation_feasible.json` | `pytest tests/test_active_set_ablation.py tests/test_ablation_feasible.py` |
| C9 | Σ_φ parameter-family sensitivity: which frozen conclusions survive replacing the single-line parameterization (forced logI/dir variance ratio ~3283) with a 3-axis family (independent channel ratio, per-light heterogeneity, channel coupling) | **D robust**: direction share of the budget value at the operating point 0.07% (median; all 11 objects censored-high r* > 25° on the σ_dir sweep, direction-only D ≤ 1.68% over the whole family grid — the C7 measurement generalizes, the ratio was never the mechanism); reverse control at σ_dir=1°: intensity share ≥ 0.863 at every σ_logI. **A two-term law: parameterization-specific, failure region = the direction-only region** — median |dV| 0.0076 (within the preregistered 0.01) but max 0.464; wherever the intensity channel carries the nuisance the law stays at frozen accuracy (worst intensity-carrying cell 0.048, ~the frozen envelope 0.039), and the blow-ups sit exactly in the direction-only shapes D already excludes — the law is valid in the region that carries the budget value (identity part Σ-free: gauge residual < 1e-9 per object). **B ceiling**: runtime guard over all 495 rows, max violation −0.0028 (theorem M10/M10′, zero Σ-dependence — stated in methods §7, not billed as evidence). **C linearization radius: channel-dependent** — the joint control reproduces the frozen radius bit-exactly (1.0/1.5) and intensity_only is identical; direction_only never leaves first-order scaling on [0.05, 8] (0/11 crossed — bounded rotations); het=1.0 shrinks the radius to 0.2/0.35 — the round's new physical finding: per-light calibration heterogeneity (5× spread in effective corruption scale) breaks the nominal linearization ~5× earlier than any uniform-scale corruption of the same total variance. **E informed-only ordering: parameterization-specific, and NOT a sigma-misspecification artifact** — anchor control reproduces the frozen subset bit-exactly (88/125 = 0.704); dir_heavy (σ_dir = 5°) raises the agreement to 0.889 [0.817, 0.939]; het = 1.0 breaks it below chance at 0.353 [0.268, 0.446]. Cross diagnosis (P-SIGMA-FAMILY-E-DIAG, zero-cost on the committed rows): handing the predictor the TRUE per-light σ changes its policy ordering by only 0.05 in rank correlation (S_pred = 0.949) while the realized-benefit ordering is fully re-ordered by heterogeneity (S_real = −0.325), and the matched predictor (fit_het = −0.778) performs the same as the anchor's uniform one on het truth (cross_ah = −0.738; symmetric in the other direction) — the failure is truth-end decoupling (criterion's ordering insensitive to heterogeneity, realized benefits highly sensitive), so the per-light J_A ordering loses decision validity under per-light heterogeneity regardless of σ specification (reduced power-maximal design: budgets {14, 28} carry 100% of the dp≠0 pairs) | `results/openillumination/corruption_family_sensitivity.json` + `results/magnitude/linearization_radius_family.json` + `results/openillumination/decision_quality_family.json` + `results/openillumination/corruption_family_e_diag.json` | `pytest tests/test_corruption_family_sensitivity.py` |
| C10 | **Channel-conditionality criterion for the value of better calibration, anchored to a real sphere calibration** (DiLiGenT ballPNG, 96 lights; caveat: the measured anchor's 36% direction share is dataset- and object-conditional — at the same profile DiLiGenT objects give a median ≈0 with cat/pot1 as high objects; see C11) — WHERE the budget value lands is determined by the calibration procedure's error profile, measured, not assumed | Standard alternating-LSQ sphere photometric-stereo calibration on ballPNG (estimator never sees GT; GT enters only the preregistered lit mask and the comparison): per-light direction error RMS **2.96°** (median 2.96, max 4.47), log-intensity std **0.0159** after log-domain scalar rescale — the real procedure's direction variance is **~10.6×** its intensity variance (radians vs logI), the OPPOSITE of the joint parameterization's forced 3283. Read through the frozen family machinery on the OpenIllumination cohort: at the measured anchor the direction channel carries a median **36.0%** of the budget value (per-object 4.8%–71.4%) — outcome **D-flipped** (preregistered rule: ≥2% flips). The result is a **channel-conditionality criterion**: the share of the budget value carried by each nuisance channel is a function of the calibration procedure's error profile (measurable per procedure by a sphere calibration), not a universal constant — C7's intensity dominance is the criterion's value in the intensity-dominated region, and the sphere anchor demonstrates the other region. Both regions of the criterion are now empirically instantiated (synthetic family sweep + real measured anchor). Both the measured anchor and the D-confirmed/D-flipped rule were preregistered (configs/ball_anchor.yaml) | `results/openillumination/ball_anchor.json` | `pytest tests/test_ball_anchor.py` |
| C11 | Second-dataset transfer (DiLiGenT, 10 objects / 96 lights), **loader-corrected**: the linearization envelope transfers exactly and the intensity-dominance channel split also transfers — an earlier "channel split does NOT transfer" reading was a loader artifact (missing per-light intensity normalization, 15.6–26.3° nominal-vs-GT bias; see P-DILIGENT-LOADER-FIX) | **Radius transfers (hardened)**: radius_2x median 1.0 (objects in [0.75, 1.5]), **radius_10x = 1.5 on 10/10 objects** — identical to OpenIllumination across two independent acquisition systems, robust to the loader correction. **Channel split transfers**: with the corrected loader the DiLiGenT direction channel is tiny (max D 0.20% vs OI's 1.68%) and the intensity channel reproduces joint to <0.05 pp at every level — the same intensity dominance as OI. The pre-correction values (direction max 51.2%, pot1/pot2 anchor shares ≈86%) were artifacts of the 15.6–26.3° nominal bias and are WITHDRAWN. **What remains conditional**: at the measured ball-anchor profile the direction share is object-dependent and differs between cohorts (OI median 36.0% over 4.8–71.4%; DiLiGenT median ≈0 with cat 22.1%/pot1 29.5% as the high objects) — the criterion's object axis survives; the dataset-channel-split axis is retracted. **Mechanism (P-ANCHOR-MECHANISM, OI-internal):** within OpenIllumination (11 objects, all shares at interpretable magnitude) the object-level direction share is monotone in a scene statistic computed WITHOUT the D functional — the mass the bottom-5 weak-mode subspace places on the direction parameter axes vs the intensity axis: **Spearman −0.927, n=11**. A secondary correlate is the Finf spread (−0.618 OI). **The cross-cohort statement is NOT supported**: DiLiGenT contributes only 3 objects with shares above the degenerate scale (|share| < 0.01; the other 7 are 0/0-shaped, flagged `degenerate_share` exactly as `diligent_queue.json` flags its `degenerate_objects`), and on the valid-sample pooled set (n=14) the correlation falls to **−0.688, below the preregistered 0.7 threshold** (the all-21 value −0.899 counted the 0/0 points as ordinary ranks; the preregistered rule's v1 form is retained as reference only). Light-direction spread/isotropy and the gauge-coupling cosine do not reach the threshold on any caliber. So: the OI-internal mechanism stands (a computable scene statistic predicts the per-object direction share at ρ≈−0.93); whether it transfers across cohorts remains open for want of DiLiGenT objects at interpretable share magnitude. **Scope note (the het finding is OI-only):** the E-arm heterogeneity result was measured on OpenIllumination only — DiLiGenT never tested it, and the OI loader (used by that arm) was independently re-verified in the loader-fix round, so the result is not exposed to the DiLiGenT loader defect | `results/diligent/diligent_queue.json` + `results/diligent/provenance/loader_normalization_fix.json` | `pytest tests/test_diligent_queue.py tests/test_diligent_loader_consistency.py` |
| C12 | Literature-style selection baselines **on the normal-angular-error endpoint**, active-set-controlled, **loader-corrected**: geometry-only selection carries real value on BOTH cohorts; the calibrated model adds a large margin on OI and only a marginal one on DiLiGenT | v1.2 (DiLiGenT loader corrected — the v1.1 "model's ordering does not transfer / a_opt loses to random" reading was an artifact of the missing per-light intensity normalization and is WITHDRAWN; see P-DILIGENT-LOADER-FIX): **OpenIllumination — geometry-insufficient** (dc05_active −0.438/−0.615 vs informed −0.704/−1.204; every informed policy beats geometry) and **DiLiGenT — geometry-informative at k=14 (both beat random: dc05_active −0.336, informed −0.274) / geometry-insufficient at k=28** (dc05_active −0.335, informed −0.369). With the corrected loader **both geometry-only and the informed family beat active-restricted random on DiLiGenT at both budgets**; the model's incremental margin over geometry is large on OI and marginal (≤0.06°) on DQ. OI informed rows bit-identical to the frozen E-arm subset (88/88). Gardi 2022 / ReLeaPS 2023 not reimplemented (objective differs; comparability registered) | `results/baseline/baseline_comparison.json` + `results/diligent/provenance/loader_normalization_fix.json` | `pytest tests/test_baseline_comparison.py` |

## Registered negative results

| # | Finding | Evidence |
|---|---|---|
| N-1 | E-optimal gains violate submodularity (1518 triples over 10 instances, γ_min = 0.704); A-optimal at most marginal (γ_min ≥ 0.99989); D-optimal clean | `results/submodularity/submodularity_search.json` |
| N-2 | The stratified (cross-object) severity comparison is retired: the corrected-interface factorial flips its sign, and P_mode ≡ P_emin is a scalarization identity, not an independent predictor | `results/openillumination/correctness/mf0_factorial_summary.json` |
| N-3 | Three dynamic-range numbers, one parameter: the assessment report's 4.02% was measured on a *generic synthetic scene at `sig=0.1`* (`reconciliation.json` records the probe parameters). The present repo's 62.87% is the same functional at `level=0.5`. A **matched-heterogeneity control** holds Finf heterogeneity fixed (`obj_11_pine` het = 10.20 vs probe scene het = 11.81): at the same level (0.1) the synthetic-vs-real effect is only **0.65×** (the real object is *lower*), while changing level 0.1→0.5 on the same object gives **13.09×**. The dominant driver is therefore the `level` (calibration-uncertainty) operating point, **not** Finf heterogeneity. Consequence: the dynamic range must be reported as a **curve in `level`**, not as a single number | `results/certification/provenance/reconciliation.json`, `results/certification/certified_gaps.json`, `results/certification/lowrank_fullres.json` |

## Two pipeline versions

Two versions exist for the real-data pipeline: `legacy` (bit-reproduces the
original frozen benchmark artifacts) and `corrected` (the mathematically
documented noise-fit coefficient order and the normalized dual-coordinate
mode projection). Reported numbers use `corrected`; `legacy` outputs are kept for
reference. The preregistered A/B/C/D factorial that motivates
this is documented in `docs/EXPERIMENTS.md` and its evidence gated by
`tests/test_factorial_evidence.py`.
